{% extends 'base.html.twig' %}

{% block title %}Adresses - La Boutique{% endblock %}

{% block body %}
  <div class="py-5">
    <a href="{{ path('app_account_address_ajouter') }}" class="btn btn-info float-right">Ajouter une addresse</a>
    <h1>Mes adresses</h1><br>
        C'est dans cet espace que vous pouvez gérer les addresses
    <hr>
    <a href="{{path('app_account')}}">Retour</a>
    <hr>
    {% if app.user.addresses|length == 0 %}
       <p class="text-center">
        Vous n'avez pas ajouté des addresses sur votre compte client
       </p> 
    {% else %}
               <div class="row">
                                {% for addresse in app.user.addresses %}
                                    <div class="col-md-4 ">
                                        <div class="card bg-light">
                                           <div class="card-body">
                                                  <h5>{{ addresse.name }}</h5>
                                                  <p>{{ addresse.addresse }}
                                                      {{ addresse.company }}
                                                  </p>
                                                  <a href="{{ path('app_account_address_modifier', {'id': addresse.id })}}" class="btn btn-info">Modifier</a>|
                                                  <a href="{{ path('app_account_address_supprimer', {'id': addresse.id }) }}" class="btn btn-danger">Supprimer</a>
                                           
                                           </div>
                                        </div>
                                    </div>
                                {% endfor %}
                </div> 
    {% endif %}

        
  </div>
{% endblock %}
