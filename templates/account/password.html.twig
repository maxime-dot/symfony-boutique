{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe - La Boutique{% endblock %}

{% block body %}
    <div class="py-5">
            <h1>Modifier le mot de passe</h1>
    <a href="{{ path('app_account') }}">Retourner à l'espace membre</a>
    <br><br>
    {{ form_start(form) }}
     {{ form_widget(form) }}
    {{ form_end(form) }}
    
    
    
    </div>
{% endblock %}